{
  "name": "cari-studio-shell",
  "version": "0.1.0",
  "private": true,
  "description": "Local Windows control shell for Cari Studio native media engine.",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "check": "node --check main.js && node --check runtime/native-engine.js && node --check runtime/obs-service.js && node --check avatar-overlay-preload.js && node scripts/check-esm.cjs",
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "1.0.1",
    "obs-websocket-js": "5.0.8",
    "three": "0.186.0"
  },
  "devDependencies": {
    "electron": "44.4.2"
  }
}
